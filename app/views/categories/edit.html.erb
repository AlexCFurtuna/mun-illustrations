<h2>Edit Exhibition</h2>
  <%= form_for @category do |f| %>
    <!-- Check for errors -->
      <% if @category.errors.any? %>
        <% @category.errors.full_messages.each do |msg| %>
          <!-- Show errors -->
          <div class="alert alert-danger"> <%= msg %> </div>
        <% end %>
      <% end %>

      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <br>

      <%= f.submit "Submit", class: "btn btn-primary" %>
      <%= link_to "Cancel", categories_path, class: "btn btn-danger" %>
    <% end %>

    <h3 class"page-header"> Images in <%= @category.name %></h3>

    <div class="transitions-enabled" id="images">
      <% @images.each do |image| %>
        <div class="box panel panel-default">
          <%= link_to (image_tag image.image.url), image %>
          <h2> <%= link_to image.image_title, image %> </h2>
          <center>
            <%= link_to "Delete", image_path(image), class: "btn btn-danger", method: :delete, data: {confirm: "Are you sure?"} %>
          </center>
        </div>
      <% end %>
    </div>

    <style media="screen">

    </style>
